@model GDG_DashBoard.ViewModels.Auth.SetPasswordViewModel
@{
    Layout = null;
    ViewData["Title"] = "Set Password";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Set Password – GDG Dashboard</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms"></script>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;700;800&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    <link rel="icon" type="image/png" href="~/images/logo.png" />
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        "primary": "#4285f4",
                        "gdg-blue": "#4285f4",
                        "gdg-green": "#34a853",
                        "gdg-yellow": "#f9ab00",
                        "gdg-red": "#ea4335",
                        "on-surface": "#1b1b1c",
                        "surface": "#fcf9f8",
                        "outline-variant": "#c2c6d5",
                        "surface-container": "#f0eded",
                    },
                    fontFamily: {
                        "headline": ["Manrope", "sans-serif"],
                        "body": ["Inter", "sans-serif"]
                    }
                }
            }
        }
    </script>
    <style>
        .material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24; }
        body { font-family: 'Inter', sans-serif; }
        .gdg-dot { width: 8px; height: 8px; border-radius: 50%; }
    </style>
</head>
<body class="min-h-screen bg-surface text-on-surface antialiased">
<div class="min-h-screen flex">

    <!-- ══ LEFT PANEL — BRANDING ══════════════════════════════════════════════ -->
    <div class="hidden lg:flex lg:w-1/2 bg-on-surface flex-col justify-between p-16 relative overflow-hidden">
        <!-- Decorative blobs -->
        <div class="absolute -top-24 -left-24 w-96 h-96 bg-gdg-blue/20 rounded-full blur-3xl"></div>
        <div class="absolute bottom-0 right-0 w-64 h-64 bg-gdg-green/20 rounded-full blur-3xl"></div>
        <div class="absolute top-1/2 left-1/2 w-48 h-48 bg-gdg-yellow/10 rounded-full blur-2xl"></div>

        <!-- Logo -->
        <div class="relative z-10 flex items-center gap-3">
            <img src="~/image/gdg_bandung_logo_only.png.png" asp-append-version="true" alt="GDG Logo" class="h-10 w-auto" onerror="this.src='https://via.placeholder.com/40x40?text=G'" />
            <div>
                <p class="text-white font-bold font-headline text-lg leading-none">GDG Dashboard</p>
                <p class="text-white/40 text-[10px] font-bold uppercase tracking-widest mt-0.5">Google Developer Groups</p>
            </div>
        </div>

        <!-- Center text -->
        <div class="relative z-10">
            <div class="flex items-center gap-2 mb-6">
                <div class="gdg-dot bg-gdg-blue"></div>
                <div class="gdg-dot bg-gdg-red"></div>
                <div class="gdg-dot bg-gdg-yellow"></div>
                <div class="gdg-dot bg-gdg-green"></div>
            </div>
            <h1 class="text-5xl font-extrabold font-headline text-white leading-tight tracking-tight mb-4">
                Let's Get<br />Started.
            </h1>
            <p class="text-white/50 text-base leading-relaxed max-w-sm">
                Set up your password to unlock your internal GDG learning path and dashboard progress.
            </p>
        </div>
        
        <div class="relative z-10 min-h-[4rem]"></div>
    </div>

    <!-- ══ RIGHT PANEL — FORM ══════════════════════════════════════════════════ -->
    <div class="flex-1 flex items-center justify-center px-8 py-16 bg-surface">
        <div class="w-full max-w-md">

            <!-- Mobile logo -->
            <div class="lg:hidden flex items-center gap-2 mb-8">
                <img src="~/images/logo.png" asp-append-version="true" alt="GDG Logo" class="h-8 w-auto" onerror="this.src='https://via.placeholder.com/32x32?text=G'" />
                <p class="font-bold font-headline text-on-surface">GDG Dashboard</p>
            </div>

            <h2 class="text-3xl font-extrabold font-headline tracking-tight text-on-surface mb-1">Set Your Password</h2>
            <p class="text-on-surface/50 text-sm mb-10">Choose a secure password for your new account.</p>

            <!-- Validation summary -->
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="mb-6 bg-gdg-red/5 border border-gdg-red/20 rounded-xl p-4 flex items-start gap-3">
                    <span class="material-symbols-outlined text-gdg-red text-lg mt-0.5">error</span>
                    <div asp-validation-summary="All" class="text-sm text-gdg-red font-medium"></div>
                </div>
            }

            <form asp-controller="Auth" asp-action="SetPassword" method="post" class="space-y-6">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="Email" />
                <input type="hidden" asp-for="Token" />

                <!-- Password -->
                <div>
                    <label asp-for="Password" class="block text-xs font-bold uppercase tracking-widest text-on-surface/50 mb-2"></label>
                    <div class="relative">
                        <span class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-outline text-xl">lock</span>
                        <input asp-for="Password" type="password"
                               class="w-full pl-12 pr-12 py-3.5 bg-surface-container border border-outline-variant rounded-xl text-sm text-on-surface placeholder:text-on-surface/30 focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary transition-all"
                               placeholder="••••••••" />
                        <button type="button" onclick="togglePassword(this)"
                                class="absolute right-4 top-1/2 -translate-y-1/2 text-outline hover:text-on-surface transition-colors">
                            <span class="material-symbols-outlined text-xl">visibility</span>
                        </button>
                    </div>
                </div>

                <!-- Confirm Password -->
                <div>
                    <label asp-for="ConfirmPassword" class="block text-xs font-bold uppercase tracking-widest text-on-surface/50 mb-2"></label>
                    <div class="relative">
                        <span class="material-symbols-outlined absolute left-4 top-1/2 -translate-y-1/2 text-outline text-xl">lock_reset</span>
                        <input asp-for="ConfirmPassword" type="password"
                               class="w-full pl-12 pr-12 py-3.5 bg-surface-container border border-outline-variant rounded-xl text-sm text-on-surface placeholder:text-on-surface/30 focus:outline-none focus:ring-2 focus:ring-primary/30 focus:border-primary transition-all"
                               placeholder="••••••••" />
                        <button type="button" onclick="togglePassword(this)"
                                class="absolute right-4 top-1/2 -translate-y-1/2 text-outline hover:text-on-surface transition-colors">
                            <span class="material-symbols-outlined text-xl">visibility</span>
                        </button>
                    </div>
                </div>

                <!-- Submit -->
                <button type="submit"
                        class="w-full py-4 bg-gdg-blue text-white font-bold rounded-xl text-sm shadow-lg hover:bg-[#1a73e8] hover:shadow-xl active:scale-[0.98] transition-all flex items-center justify-center gap-2 mt-4">
                    <span class="material-symbols-outlined text-sm" style="font-variation-settings:'FILL' 1;">check_circle</span>
                    Save Password
                </button>
            </form>
        </div>
    </div>
</div>

<script>
    function togglePassword(btn) {
        const input = btn.closest('.relative').querySelector('input');
        const icon = btn.querySelector('.material-symbols-outlined');
        if (input.type === 'password') {
            input.type = 'text';
            icon.textContent = 'visibility_off';
        } else {
            input.type = 'password';
            icon.textContent = 'visibility';
        }
    }
</script>
</body>
</html>
